#ifndef UNDO_H
#define UNDO_H

#include <Macros.h>

typedef struct{
	uchar * addr;
	uchar val;
}undo_action;


/* 
undo_buffer init_undo(uchar _l){
	undo_buffer result;
	
	return result;
}
 */
#endif